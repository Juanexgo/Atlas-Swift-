# Atlas web — environment configuration
#
# Copy to `.env.local` for dev overrides. Defaults work without any env.

# URL of the Atlas API (used for AI summaries + realtime sync).
# Leave empty to run in pure-offline mode (Yjs+IndexedDB only).
NEXT_PUBLIC_ATLAS_API_URL=http://localhost:4001
